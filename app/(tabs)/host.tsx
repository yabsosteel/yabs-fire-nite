import { View, Text } from "react-native";

export default function HostScreen() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text style={{ color: "#fff" }}>Host panel coming next</Text>
    </View>
  );
}